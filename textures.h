#ifndef TEXTURES_H
#define TEXTURES_H

// set my preferred texture options (see .c file for details)

void set_texture_options(void);

// generates 2D texture from filepath

unsigned int get_texture(const char *);

#endif // TEXTURES_H
